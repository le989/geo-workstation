# Shared Package

`@geo-workstation/shared` stores small cross-application constants and types.

Phase 0 only includes the shared API response shape and app name. GEO business enums, DTO-facing types, and model constants should be added during later phases when the corresponding backend and frontend modules are implemented.
